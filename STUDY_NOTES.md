# 《高等数值分析》数学基础知识笔记

> 根据 `MathBasics.pdf` 整理。网页版位于 `dist/index.html`，其中包含可点击知识图谱、章节导航与自测题。

## 总体知识链

线性空间（允许线性组合） → 度量（定义距离与收敛） → 范数（定义大小与误差） → 内积（定义角度、正交与投影） → 矩阵范数（分析误差放大与稳定性）。

完备性贯穿度量空间和赋范空间：完备的赋范空间称为 Banach 空间。

## 1. 线性空间

- 线性空间在数域 `𝔽` 上对加法和数乘封闭，并满足交换、结合、分配、零元和逆元等公理。
- “向量”不只表示坐标数组；矩阵、多项式、连续函数也可作为线性空间中的元素。
- 若 `a₁x₁+⋯+aₙxₙ=0` 仅有零系数解，则 `{xᵢ}` 线性无关。
- 一组既线性无关又能张成整个空间的向量称为基，基中元素个数称为维数。
- `Pₙ={次数不超过 n 的多项式}` 的自然基是 `{1,x,…,xⁿ}`，所以 `dim Pₙ=n+1`；`C[a,b]` 是无限维空间。

数值分析常把无限维问题限制到有限维子空间，用有限组基函数表示近似解。

## 2. 度量与完备性

度量 `d(x,y)` 满足：

1. `d(x,y)≥0` 且 `d(x,y)=0 ⇔ x=y`；
2. `d(x,y)=d(y,x)`；
3. `d(x,z)≤d(x,y)+d(y,z)`。

Cauchy 序列的各项在序列内部最终任意接近，它不需要预先知道极限。若空间中每个 Cauchy 序列都收敛到空间内的点，则空间完备。完备化可把 Cauchy 序列按“彼此距离趋于 0”分类，并把每个等价类视为一个新点。

## 3. 范数

范数满足正定性、齐次性与三角不等式，并诱导距离 `d(x,y)=∥x−y∥`。

- `∥x∥∞=maxᵢ|xᵢ|`：控制最坏分量；
- `∥x∥₁=Σᵢ|xᵢ|`：累计绝对量；
- `∥x∥₂=(Σᵢ|xᵢ|²)¹ᐟ²`：欧氏长度。

函数空间中相应地有最大范数、`L¹` 范数和 `L²` 范数。选择范数就是选择误差评价标准。

若存在与向量无关的常数 `C₁,C₂>0`，使 `C₁∥x∥α≤∥x∥β≤C₂∥x∥α`，则两个范数等价。有限维空间中任意范数等价；无限维空间中一般不成立。

## 4. 内积、正交与正交多项式

内积给出范数 `∥x∥=√(x,x)`，并定义正交关系 `x⊥y ⇔ (x,y)=0`。Cauchy–Schwarz 不等式为 `|(x,y)|≤∥x∥∥y∥`。

若 `{xᵢ}` 是正交基，展开 `x=Σaᵢxᵢ` 的系数可独立求得：

`aᵢ=(x,xᵢ)/(xᵢ,xᵢ)`。

Gram–Schmidt 正交化依次减去新向量在已有方向上的投影：

`yᵢ=xᵢ−Σⱼ<ᵢ[(xᵢ,yⱼ)/(yⱼ,yⱼ)]yⱼ`。

加权内积 `(f,g)ρ=∫ρ(x)f(x)g(x)dx` 产生不同的正交多项式族：

- Legendre 多项式：区间 `[-1,1]`、权函数 `ρ=1`；
- Chebyshev 多项式：权函数 `ρ=1/√(1−x²)`，`Tₙ(x)=cos(n arccos x)`。

正交多项式用于逼近、数值积分与稳定插值。

## 5. 矩阵范数与稳定性

矩阵范数需满足次乘性 `∥AB∥≤∥A∥∥B∥`。

- Frobenius 范数：`∥A∥F=(Σᵢⱼ|aᵢⱼ|²)¹ᐟ²`；
- 诱导范数：`∥A∥=supₓ≠0 ∥Ax∥/∥x∥`，即最大放大倍数；
- `∥A∥∞` 是最大绝对行和，`∥A∥₁` 是最大绝对列和，`∥A∥₂=√ρ(AᴴA)`。

谱半径 `ρ(A)=max|λᵢ|` 对任意矩阵范数满足 `ρ(A)≤∥A∥`。对任意 `ε>0`，可选择某种诱导范数使 `∥A∥≤ρ(A)+ε`。

若 `∥B∥<1`，则 `I+B` 可逆且 `∥(I+B)⁻¹∥≤1/(1−∥B∥)`。若 A 可逆且 `∥C−A∥<1/∥A⁻¹∥`，则 C 仍可逆。这些结论说明 `∥A⁻¹∥` 越大，问题对扰动越敏感，并自然引向条件数 `κ(A)=∥A∥∥A⁻¹∥`。

## 复习建议

先能口头回答每种结构“新增了什么能力”，再记公式：

1. 线性空间：可以组合；
2. 度量：可以比较距离与讨论收敛；
3. 范数：可以衡量大小与误差；
4. 内积：可以讨论角度、正交和投影；
5. 矩阵范数：可以估计算子对误差的放大。
